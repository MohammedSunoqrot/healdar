# Healdar — production dependencies
# Tested on Python 3.11 (Docker) and 3.13 (local).

streamlit==1.58.0
groq==1.4.0
chromadb==1.5.9
sentence-transformers==5.5.1
rank-bm25==0.2.2
python-dotenv==1.2.2
PyMuPDF==1.27.2.3
pypdf==6.12.2
langchain-text-splitters==1.1.2
tiktoken==0.13.0
reportlab==4.5.1
python-docx==1.2.0
requests==2.32.5
# pypdf needs it to open AES-encrypted PDFs (e.g. the UAE PDPL Arabic text).
cryptography==50.0.1

# sentence-transformers pulls torch — pin the CPU build to keep the image lean
# (CUDA torch is ~2 GB; the CPU build is ~250 MB). torchvision is deliberately
# NOT installed: this is a text-only pipeline and it adds ~30 MB for nothing.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.0+cpu
